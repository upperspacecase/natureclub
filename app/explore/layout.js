export const dynamic = "force-dynamic";

export const metadata = {
    title: "Explore — Nature Club",
    description: "Discover nature events near you.",
};

export default function ExploreLayout({ children }) {
    return children;
}
